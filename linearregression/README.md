# linear-regression

Implementation of a simple linear regression.

## Description
A basic machine learning project to predict the price of a car based on its mileage. It implements gradient descent to train the model and a prediction script to use the resulting theta values.

## Usage
Train the model:
```bash
python3 train.py
```
Predict:
```bash
python3 predict.py
```
