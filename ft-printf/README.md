# ft-printf

A custom implementation of the standard printf function.

## Description
This project involves recoding the famous `printf` function from `stdio.h`. It covers variadic arguments and various format specifiers like `%c`, `%s`, `%p`, `%d`, `%i`, `%u`, `%x`, and `%X`.

## Usage
Compile the library using:
```bash
make
```
